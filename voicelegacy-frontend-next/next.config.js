/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // Remove experimental.appDir; it's not supported here.
  experimental: {}
};

module.exports = nextConfig;
